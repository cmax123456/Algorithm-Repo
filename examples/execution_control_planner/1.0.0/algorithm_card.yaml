algorithm_id: execution_control_planner
version: 1.0.0
display_name: Execution Control Planner
backend_type: python_http_service
status: draft

task_family: planning
modalities:
  input:
    - structured_json
  output:
    - structured_json

capabilities:
  - execution_control_planner

operational_functions:
  - {function_id: KC-22, function_code: issue_order, function_name: Issue Order, role: primary, coverage_level: partial}
  - {function_id: KC-23, function_code: attack_target_threat, function_name: Attack Target / Threat, role: handoff, coverage_level: external_handoff}
  - {function_id: KC-24, function_code: track_weapon, function_name: Track Weapon, role: supporting, coverage_level: partial}

agent_view:
  summary: >
    Execution Control Planner packaged for the A2A algorithm library.
  when_to_use:
    - Structured JSON inputs are available for this algorithm family.
  when_not_to_use:
    - Raw media or unstructured text without preprocessing.
  input_description: >
    See input.schema.json for required structured fields.
  output_description: >
    See output.schema.json for structured outputs.

machine_spec:
  input_schema_ref: input.schema.json
  output_schema_ref: output.schema.json
  runtime:
    backend_type: python_http_service
    endpoint: http://127.0.0.1:9012/predict
    health_endpoint: http://127.0.0.1:9012/health
    metadata_endpoint: http://127.0.0.1:9012/metadata
    timeout_ms: 3000

constraints:
  max_input_chars: 10000
  max_request_bytes: 1048576
  batch_supported: false
  streaming_supported: false

performance:
  latency_ms_p50: 20
  latency_ms_p95: 100
  primary_metric: command_generation_success
  primary_score: 0.88

model_profile:
  parameter_count: 266
  parameter_count_text: reuses the 28 mined execution-control rules (266 stored fields) plus deterministic kinematic extrapolation
  precision: deterministic_rules

safety:
  risk_level: medium
  requires_human_review: true
